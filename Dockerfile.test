# Dockerfile ultra-simples para testes (agora usando alpine para consistência)
FROM node:20-alpine

WORKDIR /app

# Dependências de sistema necessárias (openssl, compatibilidade glibc)
RUN apk add --no-cache openssl libc6-compat

# Copia tudo
COPY . .

# Instala dependências (com legacy peer deps)
RUN npm install --legacy-peer-deps

# Gera Prisma
RUN npx prisma generate

# Build
RUN npm run build

# Expõe porta
EXPOSE 3000

# Start
CMD ["npm", "start"]
